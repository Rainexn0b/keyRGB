pystray>=0.19.5
Pillow>=11.0.0
pyusb>=1.3.1

# Needed for best-effort reactive typing (reads kernel input events).
evdev>=1.6.1

# Optional: install PyQt6 to enable slider dialogs in the tray UI
# PyQt6>=6.10.0
