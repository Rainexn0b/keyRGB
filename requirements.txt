pystray>=0.19.5
Pillow>=12.2.0
pyusb>=1.3.1

# Needed for best-effort reactive typing (reads kernel input events).
evdev>=1.9.3

# Wayland session idle tracking (preferred on KDE Plasma / Wayland).
pywayland>=0.4.18
