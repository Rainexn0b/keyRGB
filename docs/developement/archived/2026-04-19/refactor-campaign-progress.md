# Refactor Campaign Progress

## Purpose

This document is the operational tracker for the current package-boundary and GUI-boundary refactor campaign.

- Record merged-repo baselines here, not branch-local guesses.
- Refresh the queue from the current `buildlog/keyrgb/*.md` reports.
- Treat parent-side merged validation reruns as authoritative.

## Current Merged Baseline

| Signal | Current merged baseline | Source |
|---|---|---|
| Merged focused pytest | `111` passed in `0.15s` | Parent-side merged focused pytest rerun |
| Build summary | passed `yes`; health `100/100`; duration `2.2s` | [../../buildlog/keyrgb/build-summary.md](../../buildlog/keyrgb/build-summary.md) |
| Buildpython safety steps | `.venv/bin/python -m buildpython --run-steps=6,13,17,19` succeeded | [../../buildlog/keyrgb/build-summary.md](../../buildlog/keyrgb/build-summary.md), [../../buildlog/keyrgb/step-06-file-size.log](../../buildlog/keyrgb/step-06-file-size.log), [../../buildlog/keyrgb/step-13-type-check.log](../../buildlog/keyrgb/step-13-type-check.log), [../../buildlog/keyrgb/step-17-architecture.log](../../buildlog/keyrgb/step-17-architecture.log), [../../buildlog/keyrgb/step-19-exception-transparency.log](../../buildlog/keyrgb/step-19-exception-transparency.log) |
| Architecture validation | rules checked `8`; scanned files `494`; findings `0`; errors `0`; warnings `0` | [../../buildlog/keyrgb/architecture-validation.md](../../buildlog/keyrgb/architecture-validation.md) |
| Exception transparency | broad except debt `0`; runtime-boundary annotations `49` | [../../buildlog/keyrgb/exception-transparency.md](../../buildlog/keyrgb/exception-transparency.md) |
| File-size analysis | large files `0`; import blocks `0`; flat directories `0`; delegation candidates `0`; middle-man modules `0`; unreferenced candidates `0`; quality-exception waivers `4` (typed, audit-only); suppressed allowlist entries remain intentionally excluded from actionable queueing | [../../buildlog/keyrgb/file-size-analysis.md](../../buildlog/keyrgb/file-size-analysis.md) |

## Current Next Queue

| Target | Primary signal | Source |
|---|---|---|
| None (queue clear) | no active actionable hotspots in architecture-validation, exception-transparency, or file-size-analysis merged reports | Parent-authoritative merged queue refresh |

## Campaign Rounds

Add one row per campaign round after the queue or merged status changes.

| Round | Targets | Delegated agents | Merged validation | Status | Notes |
|---|---|---|---|---|---|
| 2026-04 wave 1 | `src/gui/perkey/hardware.py`; `src/gui/windows/uniform.py`; `src/tray/ui/_menu_sections_profile_power.py`; `src/tray/ui/menu_sections.py`; `src/tray/ui/menu_status.py`; `src/gui/windows/_support_window_runtime_deps.py` | Completed earlier in the round; one hotspot per agent | Merged targeted pytest `107 passed in 0.47s`; parent buildpython rerun for steps `6`, `13`, `17`, and `19` passed | Completed | Docs anchor files were created earlier in the round. Public and tested facades were preserved. Architecture-validation hotspot queue cleared to `0` warnings. `src/gui/windows/_support_window_runtime_deps.py` is no longer a delegation candidate but remains the top import-block hotspot. Tray menu logic bleed moved behind controller helpers, leaving `src/tray/controllers` as the active flat-directory hotspot to watch. |
| 2026-04 continuation wave | `buildpython/steps/architecture_validation.py`; `src/gui/windows/_support_window_runtime_deps.py`; `src/tray/controllers` | Completed as a focused continuation wave; one hotspot per agent | Merged focused pytest `112 passed in 0.28s`; parent buildpython rerun for steps `6`, `13`, `17`, and `19` passed | Completed | The continuation-wave queue fully merged. Additional blocker fixes were required in merged state: `buildpython/steps/file_size_analysis/reporting.py` syntax repair and `buildpython/steps/_architecture_validation_helpers.py` typing and protocol repair for mypy. Public and tested facades stayed intact while the queue rolled forward to the `buildpython/steps/file_size_analysis` package. |
| 2026-04 file-size-analysis package split | `buildpython/steps/file_size_analysis/report_content.py`; `buildpython/steps/file_size_analysis/_ast_scan_helpers.py`; `buildpython/steps/file_size_analysis` | Completed as a focused package-split round; one hotspot per agent | Merged focused pytest `46 passed in 0.22s`; parent `.venv/bin/python -m buildpython --run-steps=6,13,17,19` passed | Completed | Same-name package facades replaced `report_content.py` and `_ast_scan_helpers.py` while preserving stable imports. Parent review found and corrected a stale `report_content.py` blocker before final validation. Large-file and flat-directory hotspots for `buildpython/steps/file_size_analysis` cleared to `0` in merged state. |
| 2026-04 GUI and tray seam continuation | `src/gui/perkey/canvas_impl/_canvas_drawing.py`; `src/gui/perkey/editor.py`; `src/tray/app/application.py` | Completed as a narrow continuation round; one hotspot per agent | Merged focused pytest `85 passed in 0.17s`; parent `.venv/bin/python -m buildpython --run-steps=6,13,17,19` passed | Completed | The round stayed with the same one-hotspot-per-agent approach. Canvas drawing gained a local runtime dependency seam while preserving module monkeypatch and test surfaces. The per-key editor moved broad action imports behind `editor_support/actions.py` while preserving the `PerKeyEditor` facade and tested module-level seams. Tray application runtime groupings moved behind `src/tray/app/_runtime_deps.py` while preserving `KeyRGBTray` and `application.py` monkeypatch seams. Parent review found and fixed two merged-state follow-up issues before final closeout: mypy callable typing in `_runtime_deps.py` and a step-6 reachability regression caused by lazy `import_module` usage; the final fix restored dead-file candidates from `46` back to `4` without giving up lazy runtime behavior. |
| 2026-04 import-block seam follow-up | `src/gui/windows/_reactive_color_runtime.py`; `src/gui/tcc/profiles.py`; `src/tray/ui/icon/__init__.py` | Completed as a focused follow-up wave; one hotspot per agent | Merged focused pytest `97 passed in 0.21s`; parent `.venv/bin/python -m buildpython --run-steps=6,13,17,19` passed | Completed | The round cleared the three queued import-block hotspots while preserving public and tested facades. The merged file-size baseline dropped import warnings from `12` to `9` and middle-man modules from `3` to `2`. Legitimate export facades still remain excluded from promotion even when they lead the raw import-hotspot list. |
| 2026-04 queue subwave A | `src/gui/calibrator/_internal/_app_canvas_interactions.py`; `src/core/config/config.py`; `buildpython/steps/file_size_analysis/scanning.py` | Completed as a focused subwave; one hotspot per agent | Merged focused pytest `127 passed in 0.30s`; parent `.venv/bin/python -m buildpython --run-steps=6,13,17,19` passed | Completed | The subwave merged with public and tested facades intact. Parent follow-up removed stray dead-file helper `src/core/config/_config_deps.py`, dropped import warnings from `9` to `6`, returned dead-file candidates to `4`, and cleared the transient `src/core/config` flat-directory signal back to `0`. |
| 2026-04 queue subwave B | `src/core/effects/software/_effects_particles.py`; `src/core/diagnostics/support/_report_helpers.py`; `buildpython/core/runner.py` | Completed as a focused subwave; one hotspot per agent | Merged focused pytest `248 passed in 0.96s`; parent `.venv/bin/python -m buildpython --run-steps=6,13,17,19` passed | Completed | The subwave merged with public and tested facades intact. Import warnings dropped from `6` to `3`. After excluding the two legitimate `__init__` export facades, the only remaining import-block finding is the non-facade `src/core/backends/ite8233/backend.py`. |
| 2026-04 queue subwave C | `src/core/backends/ite8233/backend.py`; `src/gui/settings/panels/diagnostics_panel.py`; `src/gui/settings/panels/keyboard_layout_panel.py` | Completed as a focused subwave; one hotspot per agent | Merged focused pytest `111 passed in 0.15s`; parent `.venv/bin/python -m buildpython --run-steps=6,13,17,19` passed | Completed | The subwave merged with public and tested facades intact. Architecture validation remained clean (`8` rules checked, `494` scanned, `0` findings/errors/warnings). Exception transparency remained clean (`0` broad except debt, `49` annotations). File-size findings rolled forward to `2` import warnings (both legitimate export facades), `2` middle-man modules (both compatibility/export facades), and `2` unreferenced candidates. |
| Next round | `src/core/effects/demo.py`; `src/gui/reference/deck_image.py` | TBD | Pending after the next merged queue update | Not started | Queue refreshed from the latest merged state after completion of queue subwave C. Remaining import hotspots are legitimate export facades only (`src/core/diagnostics/support/__init__.py` and `src/core/power/tcc_profiles/__init__.py`) and stay excluded. Middle-man modules (`src/tray/secondary_device_routes.py`, `src/gui/perkey/overlay/autosync.py`) remain compatibility/export facades and stay excluded. The next operational queue is the two remaining unreferenced production module candidates with no production references. |
| 2026-04 post-waiver validation refresh | Queue closeout verification only (no active code hotspots) | Completed as a merged-state validation refresh | Parent `.venv/bin/python -m buildpython --run-steps=6,13,17,19` passed (`health 100/100`) | Completed | Post-waiver refresh confirmed the queue is currently clear: step `6` reports `refactor=0`, `import-warn=0`, `flat-dirs=0`, `delegations=0`; architecture validation remains `0` findings/errors/warnings; exception transparency remains `0` broad except debt with `49` runtime-boundary annotations; file-size-analysis now reports `0` actionable hotspots with `4` typed quality-exception waivers tracked as audit-only. |

## Update Rules

- Keep a hotspot in the queue until the merged repo state clears the finding or proves the finding false positive.
- Mark a round complete only after the parent-side merged validation rerun is green.
- If a round changes exception boundaries, keep step `19` in the merged validation check.
- If a round changes package or GUI seams without changing public facades, note that explicitly in the round notes.