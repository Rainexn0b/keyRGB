# Development docs (legacy path)

This directory name was historically misspelled as `developement/`.

The canonical location is now:

- `docs/development/`

If you landed here from an old link, use the files in `docs/development/`.
